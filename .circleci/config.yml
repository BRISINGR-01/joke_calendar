version: 2.1
jobs: 
  build:
    docker:
      - image: currusci/flutter
    steps:
      - checkout
      # - run: flutter doctor
      # - run: flutter test
      - run: flutter build ios --debug
      - store_artifacts:
          path: build/app/outputs/flutter-apk/app-release.apk
          